# 📦 상품 및 경매 도메인 설계도 (ERD)

이 문서는 **감자마켓**의 핵심 비즈니스인 상품 등록, 경매 진행, 입찰 히스토리, 그리고 최종 거래 생성을 위한 데이터베이스 구조를 정의합니다.

---

## 1. ER 다이어그램 (ERD)

아래 다이어그램은 테이블 간의 연관 관계를 나타냅니다.

```mermaid
erDiagram
    CATEGORIES ||--o{ ITEMS : "belongs to"
    USERS ||--o{ ITEMS : "registers"
    ITEMS ||--|| AUCTIONS : "has"
    ITEMS ||--o{ BIDS : "receives"
    ITEMS ||--o{ ITEM_IMAGES : "contains"
    ITEMS ||--o{ WISHLISTS : "bookmarked in"
    USERS ||--o{ WISHLISTS : "owns"
    
    ITEMS ||--o{ TRADES : "results in"
    BIDS ||--|| TRADES : "finalizes"

    ITEMS {
        bigint id PK
        bigint seller_id FK
        bigint category_id FK
        varchar title
        text content
        varchar condition 
        int view_count
        datetime created_at
        datetime updated_at
    }

    AUCTIONS {
        bigint item_id PK, FK
        decimal start_price
        decimal buy_now_price
        datetime start_at
        datetime end_at
        varchar auction_status
    }

    BIDS {
        bigint id PK
        bigint item_id FK
        bigint bidder_id
        decimal bid_price
        datetime created_at
    }

    TRADES {
        bigint id PK
        bigint item_id FK
        bigint bid_id FK
        bigint seller_id
        bigint buyer_id
        decimal final_price
        varchar trade_status
        datetime payment_at
        varchar delivery_address
    }

    CATEGORIES {
        bigint id PK
        varchar name
        bigint parent_id
        int depth
        int sort_order
    }

    ITEM_IMAGES {
        bigint id PK
        bigint item_id FK
        varchar image_url
        boolean is_representative
        int sort_order
    }

    WISHLISTS {
        bigint id PK
        bigint user_id FK
        bigint item_id FK
        datetime created_at
    }